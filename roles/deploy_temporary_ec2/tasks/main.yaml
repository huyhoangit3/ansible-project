---
- name: Find a group with matching name/prefix
  amazon.aws.autoscaling_group_info:
#    name: {{deploy_temporary_ec2_target_asg}}
    name: flets-rproxy-dev-osaka-ec2-autoscaling-gr-3a
  register: result

- set_fact: my_deploy_dir="{{ result.stdout }}"

- name: Print
  debug:
    msg: "{{ my_deploy_dir }}"